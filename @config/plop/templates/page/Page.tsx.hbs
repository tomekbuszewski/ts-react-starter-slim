/**
* @author {{ userName }}
* @since {{ created }}
*/

import { connect } from "react-redux";

import { View } from "./{{ pascalCase name }}.view";

const {{ pascalCase name }} = connect()(View);

export { {{ pascalCase name }} };
