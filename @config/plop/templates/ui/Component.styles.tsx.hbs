/**
* @author {{ userName }}
* @since {{ created }}
*/

import styled from "styled-components";
import { BaseProps as Props } from "./{{pascalCase name}}.types";

const Styled{{pascalCase name}} = styled.div<Props>``;

export { Styled{{pascalCase name}} }
