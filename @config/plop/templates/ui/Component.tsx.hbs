/**
* @author {{ userName }}
* @since {{ created }}
*/

import * as React from "react";

import { Styled{{pascalCase name}} } from "./{{pascalCase name}}.styles";
import { BaseProps as Props } from "./{{pascalCase name}}.types";

const {{pascalCase name}} = React.FunctionComponent<Props> = (props) => (
  <Styled{{pascalCase name}}>{props.children}</Styled{{pascalCase name}}>
);

export { {{pascalCase name}} }
